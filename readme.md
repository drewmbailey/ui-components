# UI Components

## Start
Make sure a local server is installed: 
```bash
npm install -g serve
```

Install project files and build:
```bash
npm install
npm run build
```

Watch for changes: 
```bash
npm run watch
```